package org.ik;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCharactersResourceIT extends CharactersResourceTest {

    // Execute the same tests but in native mode.
}